{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal local UI permissions for LLM Gateway",
  "windows": ["main"],
  "permissions": ["core:default"]
}

